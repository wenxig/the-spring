# 实机网络验证

## 提交划分

1. 修正 AT 响应缓冲语义与握手状态；核验专用 USB AT 接口，提交代码和实机证据。
2. 在 AT 控制正常后验证 PPP、HTTP 与天气轮询，按独立修复提交。

## 已知证据

- 2026-09-09 项目记录：2c7c:6002 接口 0/1 为 ECM，2 为诊断，3/4 为 AT；3 的 OUT/IN 为 0x0f/0x86，4 为 0x0a/0x81。
- 2026-09-20：P4 上接口 3 打开成功，AT 超时，回调字节数为 0；Wi-Fi 获得 IP。
- 接口 0 出现 ECM 速率通知 0x2a，接口 2 出现二进制诊断数据。
- esp_modem 1.4.3 的 got_line 回调传递累计响应（consumed + len），应用应覆盖快照。追加会重复累计。
- 当前需核验接口 4 的 AT 握手。PPP 和蜂窝 HTTP 验收待完成。
