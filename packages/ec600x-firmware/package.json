{
  "name": "@the-spring/ec600x-firmware",
  "version": "0.1.0",
  "private": true,
  "description": "QuecPython firmware project for Quectel EC600X-EVB (EC600M Cat 1 bis)",
  "type": "module",
  "scripts": {
    "build": "python3 tools/pack.py build",
    "verify": "python3 tools/pack.py verify",
    "ports": "uv run python3 tools/flash.py ports",
    "flash": "uv run python3 tools/flash.py flash",
    "format": "uv run ruff format --check src tools tests",
    "format:fix": "uv run ruff format src tools tests",
    "lint": "uv run ruff check src tools tests",
    "typecheck": "uv run pyright",
    "test": "uv run pytest",
    "check": "pnpm run format && pnpm run lint && pnpm run typecheck && pnpm run test"
  }
}
