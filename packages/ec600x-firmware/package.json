{
  "name": "@the-spring/ec600x-firmware",
  "version": "0.1.0",
  "private": true,
  "description": "QuecPython firmware project for Quectel EC600X-EVB (EC600M Cat 1 bis)",
  "type": "module",
  "scripts": {
    "build": "python3 tools/pack.py build",
    "verify": "python3 tools/pack.py verify",
    "ports": "uv run --with pyserial python3 tools/flash.py ports",
    "flash": "uv run --with pyserial python3 tools/flash.py flash",
    "test": "uv run --with pytest pytest",
    "check": "uv run --with ruff ruff check src tools tests"
  }
}